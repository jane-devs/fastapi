# Python 3.11 → 3.14: Полное руководство по миграции

**Дата создания:** Декабрь 2024  
**Для разработчиков, работающих на Python 3.11 и переходящих на 3.14**

---

## Содержание
1. [Python 3.12 - Ключевые изменения](#python-312)
2. [Python 3.13 - Ключевые изменения](#python-313)
3. [Python 3.14 - Ключевые изменения (бета)](#python-314)
4. [Критические breaking changes](#breaking-changes)
5. [Практические примеры миграции](#migration-examples)
6. [Чеклист миграции](#migration-checklist)

---

<a name="python-312"></a>
## Python 3.12 - Ключевые изменения

### 🚀 Производительность
- **На 10-60% быстрее** чем 3.11 в среднем
- Улучшенная компиляция comprehensions
- Оптимизация работы со словарями и атрибутами классов

### 📝 Новый синтаксис для Type Parameters (PEP 695)

**Было (3.11):**
```python
from typing import TypeVar, Generic

T = TypeVar('T')

class Stack(Generic[T]):
    def __init__(self) -> None:
        self.items: list[T] = []
    
    def push(self, item: T) -> None:
        self.items.append(item)
```

**Стало (3.12+):**
```python
# Более короткий и читаемый синтаксис
class Stack[T]:
    def __init__(self) -> None:
        self.items: list[T] = []
    
    def push(self, item: T) -> None:
        self.items.append(item)

# Для функций
def first[T](items: list[T]) -> T:
    return items[0]

# Для type aliases
type Point[T] = tuple[T, T]
```

**Преимущества:**
- Не нужно импортировать `TypeVar` и `Generic`
- Более естественный синтаксис
- Лучшая поддержка в IDE

### 🎯 Улучшенные сообщения об ошибках

**Было (3.11):**
```python
# При ошибке показывалась вся строка
d = {"key": "value"}
print(d["missing"])
# KeyError: 'missing'
```

**Стало (3.12):**
```python
# Показывается точное место ошибки с подсветкой
d = {"key": "value"}
print(d["missing"])
      ^^^^^^^^^^^^
# KeyError: 'missing'
# Стрелка указывает ТОЧНО на проблемное место
```

**Другие улучшения traceback:**
- Цветная подсветка в терминале
- Точное указание на подвыражение в сложных выражениях
- Лучшие сообщения для NameError, AttributeError

### 🔧 f-strings улучшения

**Можно использовать кавычки того же типа внутри:**
```python
# Было (3.11) - не работало:
# f"This is {f"nested"}"  # SyntaxError

# Стало (3.12) - работает:
f"This is {f"nested"} string"
f'This is {f'nested'} string'
```

### 📦 Comprehensions имеют собственный scope

**Было (3.11):**
```python
x = "global"
[x for x in range(5)]
print(x)  # 4 - переменная "утекла" наружу!
```

**Стало (3.12):**
```python
x = "global"
[x for x in range(5)]
print(x)  # "global" - x из comprehension не влияет на внешний x
```

**Важно для миграции:**
Если ваш код полагался на "утечку" переменных из comprehensions - он сломается!

### 🔒 Per-Interpreter GIL (экспериментально)

**Subinterpreters с отдельным GIL:**
```python
import _xxsubinterpreters as interpreters

# Каждый интерпретатор имеет свой GIL
interp = interpreters.create()
interpreters.run_string(interp, "print('Hello from subinterpreter')")
```

**Для чего:**
- Настоящая параллельность в одном процессе Python
- Пока экспериментально, нестабильное API

### 📊 Новые возможности typing

**typing.override - явное указание переопределения:**
```python
from typing import override

class Base:
    def method(self) -> int:
        return 1

class Derived(Base):
    @override  # Проверит, что метод действительно переопределяет родительский
    def method(self) -> int:
        return 2
```

### ⚠️ Удалено и deprecated в 3.12

**Удалено:**
- `asyncore`, `asynchat`, `smtpd` модули (были deprecated с 3.6)
- `distutils` (используйте `setuptools`)
- Некоторые старые `unittest` методы

**Deprecated:**
- `wave` модуль будет удален в 3.13
- `locale.resetlocale()` удален
- Некоторые устаревшие функции в `typing`

---

<a name="python-313"></a>
## Python 3.13 - Ключевые изменения

### 🚀 Экспериментальный JIT компилятор

**Copy-and-patch JIT (Just-In-Time компилятор):**
```bash
# Включается флагом при сборке
python -X jit script.py
```

**Что дает:**
- До 2-9% прирост производительности на некоторых workloads
- Пока экспериментально, нужно включать вручную
- В будущем станет стандартом

### 🎨 Улучшенный интерактивный режим (REPL)

**Новые возможности:**
- Multiline editing (редактирование нескольких строк)
- Цветная подсветка синтаксиса
- История команд с поиском (Ctrl+R)
- Автодополнение улучшено

**Цветные traceback по умолчанию:**
```python
# Теперь ошибки выводятся с цветами в терминале
# Красный - ошибки
# Синий - имена файлов
# Зеленый - номера строк
```

### 🔓 Free-threading mode (экспериментально, PEP 703)

**GIL теперь опционален!**

```bash
# Специальная сборка без GIL
python3.13t script.py  # 't' означает 'free-threaded'
```

**Что это значит:**
- Многопоточный код может работать параллельно на всех ядрах
- Требует специальной сборки Python (с суффиксом 't')
- Многие C-extensions еще не совместимы
- Пока экспериментально, но это будущее Python

**Проверка режима:**
```python
import sys
print(sys._is_gil_enabled())  # False если GIL отключен
```

### 📝 locals() теперь изменяемый

**Было (3.12 и ранее):**
```python
def func():
    x = 1
    loc = locals()
    loc['x'] = 2
    print(x)  # 1 - изменения не применились!
```

**Стало (3.13):**
```python
def func():
    x = 1
    loc = locals()
    loc['x'] = 2
    print(x)  # 2 - изменения теперь работают!
```

**Важно:** Это может сломать код, который полагался на старое поведение!

### 🔍 sys.monitoring - новый API для профилирования

**Замена старого sys.settrace:**
```python
import sys

def callback(code, instruction_offset, arg):
    print(f"Event at {instruction_offset}")

# Более производительный способ мониторинга
sys.monitoring.use_tool_id(sys.monitoring.DEBUGGER_ID, "my_debugger")
sys.monitoring.register_callback(
    sys.monitoring.DEBUGGER_ID,
    sys.monitoring.events.PY_START,
    callback
)
```

**Преимущества:**
- В разы быстрее чем sys.settrace
- Меньше оверхед для профилировщиков и дебаггеров

### ⚠️ Удалено в 3.13

**Полностью удалено:**
- `wave` модуль (был deprecated в 3.12)
- Много старых deprecated функций из `typing`
- `unittest.TestCase.assert_` методы (были deprecated 20+ лет)
- `locale.resetlocale()`

**Изменения в поведении:**
- `int()`, `float()` больше не принимают строки с пробелами в начале/конце при использовании `base` параметра
  ```python
  # Было (3.12):
  int(" 42 ", 10)  # Работало
  
  # Стало (3.13):
  int(" 42 ", 10)  # ValueError!
  int(" 42 ")      # Все еще работает (без base)
  ```

---

<a name="python-314"></a>
## Python 3.14 - Ключевые изменения (бета)

**⚠️ Внимание:** Python 3.14 еще в разработке (бета), информация может измениться!

### 🔓 Free-threading improvements

**GIL-free режим становится стабильнее:**
- Больше C-extensions поддерживают no-GIL режим
- Улучшения производительности в многопоточном коде
- По-прежнему опциональный, требует специальной сборки

### 📝 PEP 649: Lazy evaluation of annotations

**Отложенная оценка type hints:**

**Было (3.13 и ранее):**
```python
from __future__ import annotations

class Node:
    # 'Node' еще не определен, нужны кавычки или future import
    def add_child(self, child: 'Node') -> None:
        pass
```

**Стало (3.14):**
```python
# Больше не нужны кавычки или future import!
class Node:
    def add_child(self, child: Node) -> None:  # Работает!
        pass

# Аннотации не вычисляются при импорте
# Это ускоряет загрузку модулей
```

**Преимущества:**
- Быстрее загрузка модулей (аннотации не вычисляются сразу)
- Не нужны forward references в кавычках
- Можно использовать еще не определенные классы

**Получение аннотаций:**
```python
import inspect

# Старый способ (3.13):
# func.__annotations__  # Вычисленные аннотации

# Новый способ (3.14):
inspect.get_annotations(func, eval_str=True)  # Контролируемое вычисление
```

### 🚀 JIT compiler improvements

**JIT становится более агрессивным:**
- Больше оптимизаций
- Лучше работает с hot loops
- Может стать включенным по умолчанию в будущих версиях

### 🔧 Улучшения в typing module

**Новые возможности:**
- Улучшенная поддержка TypedDict
- Новые utility types
- Лучшая интеграция с рантайм-проверками

### ⚠️ Планируемые удаления в 3.14

**Будут удалены (уточняйте в финальной версии):**
- Старые deprecated API из различных модулей
- Некоторые legacy функции

---

<a name="breaking-changes"></a>
## Критические Breaking Changes (3.11 → 3.14)

### ❗ Comprehensions scope (3.12)

**Проблема:**
```python
# Python 3.11 - работало:
x = 0
[x for x in range(5)]
if x > 0:  # x = 4, условие истинно
    print("Found")

# Python 3.12+ - НЕ работает:
x = 0
[x for x in range(5)]
if x > 0:  # x = 0, условие ложно!
    print("Not found")
```

**Решение:**
Не полагайтесь на "утечку" переменных из comprehensions.

### ❗ locals() стал изменяемым (3.13)

**Проблема:**
Если ваш код полагался на то, что изменения в `locals()` не влияют на реальные переменные - он сломается.

**Решение:**
Используйте `locals().copy()` если нужна копия.

### ❗ int() с пробелами (3.13)

**Проблема:**
```python
# Python 3.12 - работало:
int(" 42 ", 10)  # 42

# Python 3.13+ - ValueError!
int(" 42 ", 10)  # ValueError: invalid literal
```

**Решение:**
```python
# Используйте strip():
int(" 42 ".strip(), 10)  # 42

# Или используйте без base:
int(" 42 ")  # Все еще работает
```

### ❗ Удаленные модули

**distutils (удален в 3.12):**
```python
# Старый код:
from distutils.core import setup  # ❌ Больше не работает

# Новый код:
from setuptools import setup  # ✅ Используйте setuptools
```

**wave (удален в 3.13):**
Если используете - найдите альтернативу или скопируйте модуль в свой проект.

### ❗ unittest методы

**Удаленные assert методы:**
```python
# Старый код (3.11):
self.assertEquals(a, b)      # ❌ Удалено
self.assertNotEquals(a, b)   # ❌ Удалено

# Новый код (3.12+):
self.assertEqual(a, b)       # ✅
self.assertNotEqual(a, b)    # ✅
```

---

<a name="migration-examples"></a>
## Практические примеры миграции

### Пример 1: Обновление Generic классов

**До (3.11):**
```python
from typing import TypeVar, Generic

T = TypeVar('T')
K = TypeVar('K')
V = TypeVar('V')

class Box(Generic[T]):
    def __init__(self, item: T):
        self.item = item

class Cache(Generic[K, V]):
    def __init__(self):
        self.storage: dict[K, V] = {}
```

**После (3.12+):**
```python
# Более чистый синтаксис
class Box[T]:
    def __init__(self, item: T):
        self.item = item

class Cache[K, V]:
    def __init__(self):
        self.storage: dict[K, V] = {}
```

### Пример 2: Исправление comprehension scope

**До (код работал на 3.11):**
```python
def process_items(items):
    results = []
    i = -1  # Инициализация
    [results.append(i) for i in range(len(items))]
    print(f"Processed {i + 1} items")  # Использование i после comprehension
    return results
```

**После (совместимо с 3.12+):**
```python
def process_items(items):
    results = []
    count = len(items)
    [results.append(i) for i in range(count)]
    print(f"Processed {count} items")  # Используем отдельную переменную
    return results

# Или лучше - используйте обычный цикл:
def process_items(items):
    results = []
    for i in range(len(items)):
        results.append(i)
    print(f"Processed {i + 1} items")
    return results
```

### Пример 3: Forward references без кавычек

**До (3.13):**
```python
from __future__ import annotations

class TreeNode:
    def __init__(self, value: int):
        self.value = value
        self.left: TreeNode | None = None   # Работает только с future import
        self.right: TreeNode | None = None
```

**После (3.14):**
```python
# future import больше не нужен!
class TreeNode:
    def __init__(self, value: int):
        self.value = value
        self.left: TreeNode | None = None   # Просто работает
        self.right: TreeNode | None = None
```

### Пример 4: Использование @override

**После (3.12+):**
```python
from typing import override

class Animal:
    def speak(self) -> str:
        return "..."

class Dog(Animal):
    @override  # Проверит что метод действительно переопределяет родительский
    def speak(self) -> str:
        return "Woof!"
    
    @override
    def spek(self) -> str:  # Опечатка! Type checker выдаст ошибку
        return "Woof!"
```

---

<a name="migration-checklist"></a>
## Чеклист миграции 3.11 → 3.14

### Перед миграцией

- [ ] **Прочитайте CHANGELOG** для каждой версии: 3.12, 3.13, 3.14
- [ ] **Проверьте зависимости** - все ли библиотеки поддерживают целевую версию?
- [ ] **Создайте тестовое окружение** с новой версией Python
- [ ] **Запустите тесты** на новой версии (сначала в безопасной среде)

### Проверка кода

- [ ] **Comprehensions:** Ищите использование переменных после list/dict/set comprehensions
  ```bash
  # Поиск потенциальных проблем:
  grep -r "\[.*for .* in .*\]" --include="*.py"
  ```

- [ ] **locals():** Проверьте все места где модифицируется `locals()`

- [ ] **int() с base:** Найдите все `int(string, base)` и добавьте `.strip()` если строка может содержать пробелы

- [ ] **Удаленные модули:**
  - Замените `distutils` на `setuptools`
  - Проверьте использование `wave`, `asyncore`, `asynchat`
  - Обновите `unittest` методы

- [ ] **Type hints:** Рассмотрите переход на новый синтаксис `class MyClass[T]:`

### После миграции

- [ ] **Запустите все тесты** включая интеграционные
- [ ] **Проверьте производительность** - 3.12+ должны быть быстрее
- [ ] **Обновите CI/CD** конфигурацию с новой версией Python
- [ ] **Обновите документацию** с указанием минимальной версии Python

### Опционально (для 3.13+)

- [ ] **Попробуйте JIT компилятор** - может дать прирост производительности
- [ ] **Изучите free-threading** если у вас много многопоточного кода
- [ ] **Используйте улучшенный REPL** для интерактивной работы

---

## Полезные команды

### Проверка совместимости

```bash
# Запуск с предупреждениями о deprecated функциях
python -W default script.py

# Все предупреждения как ошибки (строгий режим)
python -W error script.py

# Проверка синтаксиса без выполнения
python -m py_compile script.py
```

### Использование разных версий

```bash
# Если установлено несколько версий
python3.11 script.py
python3.12 script.py
python3.13 script.py
python3.14 script.py

# Через pyenv
pyenv install 3.14
pyenv local 3.14
python script.py
```

### Проверка версии в коде

```python
import sys

if sys.version_info < (3, 12):
    raise RuntimeError("Python 3.12+ required")

# Проверка конкретных возможностей
if sys.version_info >= (3, 13):
    # Используем новые возможности 3.13
    print("Using Python 3.13+ features")
```

---

## Ресурсы для дальнейшего изучения

### Официальная документация
- [What's New in Python 3.12](https://docs.python.org/3/whatsnew/3.12.html)
- [What's New in Python 3.13](https://docs.python.org/3/whatsnew/3.13.html)
- [What's New in Python 3.14](https://docs.python.org/3.14/whatsnew/3.14.html)

### PEPs (Python Enhancement Proposals)
- **PEP 695:** Type Parameter Syntax (3.12)
- **PEP 703:** Making the GIL Optional (3.13)
- **PEP 649:** Deferred Evaluation of Annotations (3.14)

### Инструменты для миграции
- **pyupgrade:** Автоматически обновляет синтаксис
  ```bash
  pip install pyupgrade
  pyupgrade --py312-plus myfile.py
  ```

- **ruff:** Быстрый линтер с проверкой совместимости
  ```bash
  pip install ruff
  ruff check --target-version=py312 .
  ```

---

## Заключение

**Основные выводы:**

1. **Python 3.12** - солидное обновление с улучшениями производительности и нового синтаксиса для generics

2. **Python 3.13** - экспериментальный JIT и опциональный GIL (революционные изменения, но пока нестабильные)

3. **Python 3.14** - продолжение развития, отложенные аннотации (PEP 649)

4. **Breaking changes** минимальны, но они есть - в основном касаются comprehensions scope и удаленных deprecated функций

5. **Миграция с 3.11** должна быть относительно безболезненной, основная работа - обновление зависимостей и проверка edge cases

**Рекомендация:**
- Для production сейчас безопаснее использовать **Python 3.12** (стабильный, быстрый, проверенный)
- **Python 3.13** можно использовать если нужны новые возможности (JIT, free-threading) и вы готовы к экспериментам
- **Python 3.14** пока в бета-версии, используйте только для тестирования

Удачи с миграцией! 🚀
